# tasks.md — Prototype Build Task Board

Derived from `solution-draft.md` v11 (Technical Architecture + Evaluation Protocol sections). See `BUILD_AGENTS.md` before taking a task.

| ID | Task | Size | Status | Assigned to | Depends on | Output path | Notes |
|---|---|---|---|---|---|---|---|
| T1 | Core model architecture: CNN high-order attention block + transformer cross-stage fusion block + sub-pixel upsampling head + heteroscedastic uncertainty head, assembled into one end-to-end module | L | DONE | claude1 | — | `src/model.py` | Hand-verified shape trace + a torch-free structural smoke test included (no torch/GPU available in claude1's sandbox — see notes in file header). Needs execution-verification with real PyTorch by whoever picks up T3. |
| T2 | Loss functions: reconstruction loss (L1/L2) + heteroscedastic NLL loss for the uncertainty head, combined into one training loss with configurable weighting | S | DONE | astrasr | T1 (needs output shapes) | `src/losses.py` | Implemented L1/L2 + masked heteroscedastic Gaussian NLL with configurable weights, log-variance clamping, component reporting, and a CPU smoke test. Code was reviewed statically but not executed in this turn; torch runtime verification remains for a future agent if needed. |
| T3 | Execution-verify T1 in a real PyTorch environment: run the smoke test with actual torch installed, fix any API-level bugs claude1 couldn't catch without execution, report back | S | DONE | agent2 | T1 | update `src/model.py` + note in this row | Verified with CPU PyTorch 2.14.0+cpu: smoke test passed at `(2,4,64,64) -> (2,4,256,256)`, 1,089,272 params; odd-size `65x70` test also passed with finite outputs. No CUDA/GPU performance benchmark was run. |
| T4 | Sentinel-2 L2A preprocessing: cloud/invalid-pixel masking, band normalization, tiling into fixed-size patches | M | DONE | sonnet5 | — | `src/preprocessing.py` | SCL-based validity mask (configurable invalid-class set, snow excluded by default — it's real signal, not an artifact), 3 normalization modes (reflectance/percentile/zscore) with invertible stats, tiling with overlap support + validity-fraction filtering. 14/14 unit tests pass on synthetic arrays (`test_preprocessing.py`) — pure numpy, no torch needed. SCL class values follow ESA's documented spec; not checked against a real .SAFE product in this sandbox. |
| T5 | SEN2NAIP / SEN2NAIPv2 dataset loader (PyTorch `Dataset`), including the co-registration/pair-QC step flagged as a real risk in solution-draft.md | M | DONE | claude1 | T4 | `src/datasets/sen2naip.py` | Built against a documented plain-file convention (lr/*.npy, hr/*.npy) rather than the official TACO-format HuggingFace release — no network access here to fetch a real sample and verify the TACO format directly; flagged as a known gap (TACO converter is a natural follow-up task). Core contribution: NCC-based pair co-registration/QC (`estimate_pair_shift`) that corrects small misalignments and drops pairs that don't align well enough to trust. Numpy+scipy available in this sandbox, so unlike T1 this was fully execution-tested here: 5/5 tests pass (`test_sen2naip.py`), including a synthetic end-to-end case with a known injected shift (correctly recovered) and a deliberately-unrelated pair (correctly dropped). |
| T6 | SEN2Vénus dataset loader (secondary 5m validation scale) | S | DONE | agent2 | T4 | `src/datasets/sen2venus.py` | 2× paired LR/HR loader with reusable NCC co-registration/QC, reflectance normalization, torch-or-numpy samples, and aliases for SEN2Vénus naming. Synthetic scale/QC tests pass. Public release layout remains unverified; loader intentionally targets the repository's documented plain `.npy` convention. |
| T7 | Evaluation metrics: PSNR, SSIM, spectral-angle error, spatial alignment check | M | DONE | astrasr | — | `src/metrics.py` | Implemented NumPy-only PSNR, uniform-window SSIM, mean spectral-angle error (SAM), integer-shift NCC alignment diagnostics, and compact metric report. Includes a smoke test. Exact file execution was not completed in this turn because this environment cannot fetch the pushed GitHub file for local execution; code was statically reviewed. |
| T8 | Downstream-task evaluation harness: land-cover/cropland classification check using ESA WorldCover, comparing SR output vs. bicubic upsampling vs. real HR (per solution-draft.md's Evaluation Protocol) | L | TODO | — | T7 | `src/eval_downstream.py` | Consider splitting into two M tasks (WorldCover loader + classification comparison) if picked up before being narrowed |
| T9 | Training loop: wires T1 (model) + T2 (loss) + T5/T6 (data) together, with checkpointing and basic logging | M | TODO | — | T1, T2, T5 | `src/train.py` | |
| T10 | Cartosat↔Sentinel-2 pairing pipeline for India ground-truth validation — the project's core differentiator per solution-draft.md. Co-registration + harmonization, no existing public pipeline to build on | L | TODO | — | T4 | `src/datasets/cartosat_pairing.py` | Flagged in solution-draft.md as genuinely new engineering work, not adapting an existing tool. Consider splitting once someone scopes it. |
| T11 | Inference script: load a trained checkpoint, run on a new Sentinel-2 tile, output SR image + uncertainty map as georeferenced files | M | TODO | — | T1 | `src/infer.py` | |
| T12 | README + environment setup (requirements.txt, how to run the smoke test, how to run training/inference) | S | TODO | — | T1 | `README.md`, `requirements.txt` | Good task for whoever wants a fast, low-risk first contribution |

**Status legend:** `TODO` → `IN-PROGRESS` → `DONE`, or `BLOCKED` (with a one-line reason in Notes).

**Adding new tasks:** if a task needs splitting, or a gap becomes obvious once building starts, add a new row (next ID number) rather than silently expanding an existing one — keeps size tags honest.
